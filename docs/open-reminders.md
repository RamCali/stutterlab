# Open Reminders

- Pick a real support inbox and set it as `SUPPORT_EMAIL`.
  - Current placeholder in `.env.example`: `support@stutterlab.com`
  - Also configure `RESEND_FROM_EMAIL` and `RESEND_API_KEY` before relying on the billing support form.
